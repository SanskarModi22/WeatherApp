import 'package:flutter/material.dart';

// Define a constant text style for displaying city name.
const kCityTextStyle = TextStyle(fontSize: 18.0, color: Colors.black);

// Define a constant text style for displaying temperature.
const kTempTextStyle = TextStyle(
  fontSize: 50.0,
  fontWeight: FontWeight.bold,
  color: Colors.white,
);

// Define a constant text style for "Sign In with Google" text.
const kSignInWithGoogleTextStyle = TextStyle(
  fontSize: 20,
  color: Colors.black,
  fontWeight: FontWeight.w500,
);
